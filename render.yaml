services:
  - type: static_site
    name: talent-matching-mvp
    env: node
    plan: free
    buildCommand: "npm install && npm run build"
    staticPublishPath: "./dist"
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html" 